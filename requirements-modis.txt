# Optional: read MODIS MAIAC HDF4 granules (backend/data_sources/modis.py).
# pyhdf does not ship wheels; pip builds from source and needs HDF4 headers/libs.
#
# macOS (Homebrew), then:
#   export HDFINC="$(brew --prefix hdf4)/include"
#   export HDFLIB="$(brew --prefix hdf4)/lib"
#   pip install -r requirements-modis.txt
#
# Linux: install your distro's libhdf4-dev (or equivalent), then pip install here.
# Conda alternative: conda install -c conda-forge pyhdf

pyhdf>=0.10.5
