# ──────────────────────────────────────────────────────────────
# Mframapa v2.0 — Python Dependencies
# Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# ── HTTP & networking ──────────────────────────────────────────
requests>=2.31.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# imported directly by our own modules, so pinned here rather than relied on as
# a fastapi transitive that a future release could drop.
pydantic>=2.0.0
starlette>=0.40.0

# ── Data I/O ──────────────────────────────────────────────────
xarray>=2023.1.0
netCDF4>=1.6.4          # ERA5 netCDF files
h5py>=3.9.0             # Sentinel-5P HDF5 files
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
shapely>=2.0.0

# ── Caching ───────────────────────────────────────────────────
redis>=5.0.0            # Redis client (Upstash compatible)

# ── Auth ──────────────────────────────────────────────────────
# supabase token verification. the [crypto] extra pulls in cryptography, which
# ES256/JWKS decoding needs. without this the api still starts and simply stops
# recognising signed-in users, so it must stay declared.
pyjwt[crypto]>=2.8.0

# ── Environment ───────────────────────────────────────────────
python-dotenv>=1.0.0

# ── CDS API (ERA5) ────────────────────────────────────────────
cdsapi>=0.6.1

# ── Satellite enrichment (optional) ───────────────────────────
# real sentinel-5p / modis maiac columns via google earth engine (pipeline/enrich_gee.py)
earthengine-api>=0.1.400

# ── Alerts & scheduling ───────────────────────────────────────
# daily episode-scan / digest cron
apscheduler>=3.10.0

# ── Monitoring ────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0  # error tracking (set SENTRY_DSN env var)

# ── Testing ───────────────────────────────────────────────────
pytest>=7.4.0
pytest-mock>=3.11.0
httpx>=0.27.0               # FastAPI TestClient dependency
